from setuptools import setup, find_packages

setup(
    name="qbitshield",
    version="0.1.0",
    author="Will Daoud",
    description="QbitShield SDK – Quantum keygen API client",
    packages=find_packages(),
    install_requires=["requests"],
    python_requires=">=3.8",
)